#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n : the character to be checked
 *
 * Return: 1 or 0 or 1 or 98
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-1 * n);
	}
	else
	{
		return (n);
	}
}
